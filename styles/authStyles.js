import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#8404aeff',
    marginTop: 10,
    textAlign: 'center',
  },
  container: {
    margin: 30,
    marginTop: 60,
    backgroundColor: '#fff',
    padding: 20,
    borderRadius: 10,
  },
});
